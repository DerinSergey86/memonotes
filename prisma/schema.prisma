generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Note {
  id        String   @id @default(uuid())
  title     String
  content   String
  tags      String
  createdAt DateTime @default(now())
}